apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stock-anomaly.fullname" . }}-test-connection"
  labels:
    {{- include "stock-anomaly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stock-anomaly.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
